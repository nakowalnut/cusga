- [x] 创建顶层基类 `CharacterBase.gd`
- [x] 定义基础属性（HP, MaxHP, 状态标志）
- [x] 实现伤害处理逻辑 (`take_damage`)
- [x] 实现受击无敌帧及状态逻辑
- [x] 修改 `player.gd` 继承 `CharacterBase`
- [x] 修改 `怪物.gd` 继承 `CharacterBase` 并适配旧接口
- [x] 成功新建 `progress.txt` 记录进度

3.18
---核心更改---
	1.功能逻辑：
		废除了滚轮切换武器的旧逻辑；
		实现了数字键 1-5 快速切换武器并直接触发攻击；
		实现了切换攻击（连携攻击）与普通攻击的逻辑区分；
		将 `player.gd` 中的攻击硬直与动画播放逻辑迁移至 `attack.gd` 状态类中。

	2.类/脚本结构：
		`player.gd` 现在仅负责输入监听与视觉更新，不再处理具体的攻击硬直细节；
		`AttackState` (attack.gd) 现在负责通过 `msg` 参数接收武器信息并管理攻击全过程。

---其他修正---
	1.代码清理：
		移除了 `player.gd` 中冗余的手动切换函数 `_switch_weapon`。
		更新了 `weapon_wheel` 数组以支持全部 5 种武器的索引访问。
