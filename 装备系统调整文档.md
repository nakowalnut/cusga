# 装备系统数值策划调整手册

本文档旨在指导策划人员如何通过修改 `.csv` 文件来调整游戏中的装备属性、套装效果以及随机词条。

## 1. 核心数据文件路径

所有配置文件均存放于项目根目录下的 `Data/` 文件夹中：
- **装备基础属性**: `Data/equipment_data.csv`
- **套装效果定义**: `Data/equipment_sets.csv`
- **随机词条库**: `Data/random_affixes.csv`

---

## 2. 装备基础属性调整 (`equipment_data.csv`)

该文件定义了单件装备的基础属性和固定效果。

### 关键字段说明
| 字段 | 说明 | 示例 / 合法值 |
| :--- | :--- | :--- |
| `id` | 装备唯一标识符。不可重复。 | `set1_head_white` |
| `set_id` | 所属套装ID。关联到 `equipment_sets.csv`。 | `set1` |
| `part` | 部位。 | `head` (头部), `body` (身体), `foot` (脚部) |
| `rarity` | 品阶。 | `white`, `blue`, `gold` |
| `effects` | **核心JSON配置**。定义该装备自带的固定效果。 | 详见 [2.1 JSON 语法](#21-effects-json-语法) |
| `raw_desc` | 装备描述。仅供参考。 | "触发连携技后，下次攻击暴击率+10%" |

### 2.1 `effects` JSON 语法
装备效果使用 JSON 数组格式定义，支持多个效果叠加。
- **被动加成示例**: `[{"trigger": "passive", "type": "stat", "stat": "speed", "value": 0.08}]`
- **条件触发示例**: `[{"trigger": "on_combo", "type": "buff", "target": "next_hit", "stat": "crit_rate", "value": 0.1}]`

---

## 3. 套装效果调整 (`equipment_sets.csv`)

该文件定义了当玩家凑齐套装时触发的特殊效果。

### 关键字段说明
| 字段 | 说明 | 示例 |
| :--- | :--- | :--- |
| `set_id` | 套装唯一ID。 | `set_1` |
| `set_name` | 套装名称。 | "套装一" |
| `trigger` | 触发时机。 | `on_combo_x3`, `on_combo`, `passive` |
| `effects` | **套装核心效果**。 | `[{"type": "next_combo_dmg_mult", "value": 2.0}]` |

---

## 4. 随机词条调整 (`random_affixes.csv`)

定义装备掉落时可能出现的随机属性池。

### 关键字段说明
| 字段 | 说明 | 注意事项 |
| :--- | :--- | :--- |
| `id` | 词条ID。 | |
| `stat_type` | 加成的属性类型。 | 必须与代码中定义的属性名一致 (如 `attack`, `crit_rate`) |
| `min_val` | 随机波动的最小值。 | `0.05` 代表 5% |
| `max_val` | 随机波动的最大值。 | `0.15` 代表 15% |
| `desc` | 文本模板。 | `{val}` 会被替换为生成的数值。 |

---

## 5. 数值 `value` 的具体含义

在 `effects` 字段中，`value` 的计算方式取决于你调整的属性类型：

### 5.1 百分比加成类 (乘法系数)
对于**基础数值较大**的属性，`value` 通常作为乘法系数叠加。
- **涉及属性**: `speed` (移动速度), `attack` (攻击力), `atk_speed` (攻速), `combo_dmg` (连携伤害) 等。
- **计算逻辑**: `最终值 = 基础值 * (1 + 装备属性A + 装备属性B ...)`
- **填表建议**: 
    - 增加 10% 填 `0.1`
    - 增加 50% 填 `0.5`
    - 减少 10% 填 `-0.1`

### 5.2 直接加成类 (加法数值)
对于**概率类**或**数值本身就是百分比**的属性，`value` 直接加到最终比例上。
- **涉及属性**: `crit_rate` (暴击率), `crit_dmg` (暴击伤害), `damage_reduction` (减伤), `def` (防御力) 等。
- **计算逻辑**: `最终值 = 基础百分比 + 装备属性A + 装备属性B ...`
- **填表建议**: 
    - 增加 5% 暴击率填 `0.05`
    - 增加 30% 暴伤填 `0.3`
    - 减少 50% 体力消耗填 `-0.5`

---

## 6. 数值调整注意事项 (必读)

1.  **备份文件**: 在进行任何大规模调整前，请备份原始 `.csv` 文件。
2.  **编码格式**: 必须保持文件编码为 **UTF-8 (无BOM)**，否则会导致中文乱码。
3.  **JSON 引用**: 在 CSV 中书写 JSON 时，**双引号需要转义**。通常在 Excel 中直接写 `"`，保存为 CSV 后会自动变成 `""` 或根据编辑器处理。请务必检查 `Data/` 目录下的原始文件格式。
    *   *示例标准格式*: `"[ {""type"": ""buff""} ]"`
4.  **属性名称规范**: 确保 `stat_type` 字段的值在所有文件中统一（例如不要有的地方写 `atk` 有的地方写 `attack`）。
5.  **重载生效**: 修改完成后，在 Godot 编辑器中通常会自动重新导入。

---

## 6. 常用属性/效果关键字对照表

| 关键字 | 说明 |
| :--- | :--- |
| `attack` | 攻击力 |
| `crit_rate` | 暴击率 |
| `crit_dmg` | 暴击伤害 |
| `speed` | 移动速度 |
| `atk_speed` | 攻击速度 |
| `damage_reduction` | 伤害减免 |
| `stamina_cost` | 体力消耗 |
